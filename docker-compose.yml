services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: isyara
      POSTGRES_USER: isyara
      POSTGRES_PASSWORD: isyara
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
